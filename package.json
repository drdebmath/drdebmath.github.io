{
  "name": "drdebmath-github-io",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css",
    "build:css": "tailwindcss -c tailwind.config.cjs -i ./src/tailwind.css -o ./assets/site.css --minify",
    "validate": "npm run build:css && node validate-site.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
